# {Project Name} — Agent Instructions

## Quick Reference
- **Stack**: {Technology stack with versions (e.g., ".NET 10, C# 14, Marten, Wolverine")}
- **Docs**: {Key documentation locations (e.g., "`docs/getting-started.md`, `docs/guides/`")}
- **Run**: {Start command} | **Test**: {Test command} | **Format**: {Format command}

## Key Rules (MUST follow)
```
✅ {GoodPattern}                  ❌ {BadPattern}
✅ {GoodPattern}                  ❌ {BadPattern}
✅ {GoodPattern}                  ❌ {BadPattern}
✅ {GoodPattern}                  ❌ {BadPattern}
```

## Common Mistakes
- ❌ {Mistake description} → {Solution or where to put it}
- ❌ {Mistake description} → {Solution with skill reference if applicable}
- ❌ {Mistake description} → {Solution}

## Project Layout
| Path | Purpose |
|------|---------|
| `{directory}/` | {Purpose description} |
| `{directory}/` | {Purpose description} |
| `{directory}/` | {Purpose description} |
| `{directory}/` | {Purpose description} |

## Skills

| Category | Skills |
|----------|--------|
| **Run** | `/{skill-1}`, `/{skill-2}` |
| **Scaffold** | `/{skill-1}`, `/{skill-2}`, `/{skill-3}` |
| **Verify** | `/{skill-1}`, `/{skill-2}` |
| **Debug** | `/{skill-1}`, `/{skill-2}` |
| **Deploy** | `/{skill-1}`, `/{skill-2}` |
| **Utility** | `/{skill-1}`, `/{skill-2}`, `/{skill-3}` |
| **Documentation** | `/{skill-1}` |

**Aliases**: `/{alias}`→{skill-slug}, `/{alias}`→{skill-slug}

## Quick Troubleshooting
- **{Problem}**: {Solution or skill to run}
- **{Problem}**: Run `/{skill-slug}`
- **{Problem}**: {Solution}
- **{Problem}**: Run `/{skill-slug}`

## Documentation Index
| Topic | Guide |
|-------|-------|
| {Topic} | `{path/to/guide.md}` |
| {Topic} | `{path/to/guide.md}` |
| {Topic} | `{path/to/guide.md}` |
| {Topic} | `{path/to/guide.md}`, `{path/to/other-guide.md}` |
